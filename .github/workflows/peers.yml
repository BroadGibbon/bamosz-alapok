name: Peer group betöltés

on:
  workflow_dispatch:
    inputs:
      file:
        description: "A feltöltött peer CSV fájl neve a repóban"
        required: false
        default: "peers.csv"

jobs:
  load:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install requests
      - name: Betöltés
        run: python load_peers.py
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          FILE: ${{ github.event.inputs.file }}
